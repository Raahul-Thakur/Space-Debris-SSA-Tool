# Core runtime dependencies (see pyproject.toml for the canonical list).
# Install the package instead with:  pip install -e ".[app,dev,docs]"
numpy>=1.24
scipy>=1.10
pandas>=2.0
pyarrow>=12
sgp4>=2.23
astropy>=5.3
requests>=2.28
pydantic>=2.5
pyyaml>=6.0
click>=8.1
rich>=13.0
# Web app (Phase 3)
streamlit>=1.30
plotly>=5.18
# Ontology API (install with `pip install -e ".[api]"`)
fastapi>=0.115,<1
sqlalchemy>=2.0,<3
alembic>=1.13,<2
uvicorn>=0.30,<1
PyJWT[crypto]>=2.9,<3
psycopg[binary]>=3.2,<4
dramatiq[redis]>=1.17,<2
